defaults.url=https://sentry.io/
defaults.org=hobby-1f
defaults.project=android

# Auth token used by sentry.gradle / sentry-cli to upload source maps & native symbols.
# Prefer CI secret SENTRY_AUTH_TOKEN over committing a token here.
auth.token=
